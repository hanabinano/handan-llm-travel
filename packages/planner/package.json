{
  "name": "@handan/planner",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "echo 'planner lint placeholder'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "format": "echo 'planner format handled at repo level'"
  },
  "dependencies": {
    "@handan/shared": "workspace:*"
  }
}
